apiVersion: apps/v1
kind: Deployment
metadata:
  name: timekeeper
  labels:
    app: timekeeper
spec:
  replicas: 3
  selector:
    matchLabels:
      app: timekeeper
  template:
    metadata:
      labels:
        app: timekeeper
    spec:
      containers:
      - name: timekeeper
        image: 637321195426.dkr.ecr.us-west-2.amazonaws.com/avi-liatrio-interview@sha256:4aea94705d9853b2143ef1c6cd7cc643a6a7f2d9dc69c2c1d2f4da2fb7aa4185
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: "app.kubernetes.io/name"
                    operator: In
                    values:
                      - timekeeper
              topologyKey: "topology.kubernetes.io/zone"
